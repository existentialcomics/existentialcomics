{% extends "base.html" %}

{% block title %}Archive{% endblock %}
{% block head %}
<link rel="alternate" type="application/rss+xml" title="RSS" href="/rss.xml"/>
<link rel="shortcut icon" href="{{ static }}favicon.ico">
{% endblock %}
{% block content %}
<div id="content" class="page">
    <a href="/"><img src="{{ static }}/title.jpg" alt="A philosophy webcomic about the inevitable anguish of living a brief life in an absurd world. Also Jokes"></a>
    <div id="serious-comics" class="column">
    <h3>Existential comics:</h3>
    <ul>
    {% for comic in seriousComics %}
        <li> <a href = "{{ comic.link }}"}>{{ comic.title }}</a>{% for lang in comic.altLangs %}{% if loop.first %} ({% endif %}<a href="{{ lang.link }}">{{ lang.langName }}</a>{% if loop.first %}){% endif %}{% endfor %}</li>
    {% endfor %}
    </ul>
    </div>
    <div id="dialogs" class="column">
    <h3>Dialogs:</h3>
    <ul>
    {% for comic in dialogComics %}
        <li> <a href = "{{ comic.link }}"}>{{ comic.title }}</a>{% for lang in comic.altLangs %}{% if loop.first %} ({% endif %}<a href="{{ lang.link }}">{{ lang.langName }}</a>{% if loop.first %}){% endif %}{% endfor %}</li>
    {% endfor %}
    </ul>
    </div>
	<br>
    <div id="philosophersplay-comics" class="column">
    <h3>Philosophers play X:</h3>
    <ul>
    {% for comic in philosophersPlayComics %}
        <li> <a href = "{{ comic.link }}"}>{{ comic.title }}</a>{% for lang in comic.altLangs %}{% if loop.first %} ({% endif %}<a href="{{ lang.link }}">{{ lang.langName }}</a>{% if loop.first %}){% endif %}{% endfor %} </li>
    {% endfor %}
    </ul>
    </div>

    <div id="shitty-comics" class="column">
    <h3>Terrible philosophy jokes:</h3>
    <ul>
    {% for comic in jokeComics %}
        <li> <a href = "{{ comic.link }}"}>{{ comic.title }}</a>{% for lang in comic.altLangs %}{% if loop.first %} ({% endif %}<a href="{{ lang.link }}">{{ lang.langName }}</a>{% if loop.first %}){% endif %}{% endfor %} </li>
    {% endfor %}
    </ul>
    </div>


    <div id="bottom">
        <div class="footer">
            <p>
            Contact: <a href="mailto:existentialcomics@gmail.com?subject=You're the greatest!">existentialcomics@gmail.com</a>
            </p>
            <ul class="nav">
	      <li> <a href="/">Home</a> </li>
              <li> Archive </li>
            </ul>
        </div>
    </div>
</div>

{% endblock %}

